# All optional — sensible defaults are baked into the code.

# VITE_API_BASE_URL=

# Google sign-in (optional — omit entirely to leave it disabled). Must be
# the *same* Client ID as backend/.env's GOOGLE_CLIENT_ID.
# VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
